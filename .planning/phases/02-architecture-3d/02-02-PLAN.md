---
phase: 02-architecture-3d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/exceptions.py
  - src/core/logging.py
  - src/core/config.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "All modules use unified error handling and logging"
    - "Invalid inputs raise custom BuildScaleError with clear messages"
    - "All print() statements replaced with structured logging"
  artifacts:
    - path: "src/core/exceptions.py"
      provides: "Custom exception hierarchy (BaseError, ValidationError, etc.)"
      min_lines: 30
    - path: "src/core/logging.py"
      provides: "Logging configuration with levels and structured formatting"
      exports: ["setup_logging", "get_logger"]
    - path: "src/core/config.py"
      provides: "Environment-based configuration loading"
      exports: ["load_config", "Config"]
  key_links:
    - from: "src/core/exceptions.py"
      to: "ceiling_panel_calc.py"
      via: "from src.core.exceptions import ValidationError"
      pattern: "ValidationError|BuildScaleError"
    - from: "src/core/logging.py"
      to: "ceiling_panel_calc.py"
      via: "logger = get_logger(__name__)"
      pattern: "logger\\..*\\(.*\\)"
    - from: "src/core/config.py"
      to: "ceiling_panel_calc.py"
      via: "config = load_config()"
      pattern: "config\\.|env_var"
---

<objective>
Create unified foundational systems for error handling, logging, and configuration.

Purpose: Establish consistent error handling and logging across the entire system, replacing scattered print statements and inconsistent exceptions with structured, maintainable patterns.
Output: Core infrastructure modules that all other components will use.
</objective>

<execution_context>
@/home/tomas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/tomas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-architecture-3d/02-RESEARCH.md
@.planning/phases/02-architecture-3d/02-ARCHITECTURE-SCOUT-RESEARCH-LOGGING.md
@/home/tomas/Ceiling Panel Spacer/ROADMAP.md
@/home/tomas/Ceiling Panel Spacer/REQUIREMENTS.md
@/home/tomas/Ceiling Panel Spacer/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified exception hierarchy</name>
  <files>src/core/exceptions.py</files>
  <action>Create src/core/exceptions.py with base BuildScaleError class and specific exceptions (ValidationError, CalculationError, DatabaseError, RenderingError). Each exception includes context for debugging. Add error code mapping for frontend handling.</action>
  <verify>Import exceptions module successfully, instantiate ValidationError with message and context, verify inheritance</verify>
  <done>src/core/exceptions.py exists with complete exception hierarchy and error codes</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured logging system</name>
  <files>src/core/logging.py</files>
  <action>Create src/core/logging.py with setup_logging() function using Python logging module, configurable log levels, structured format with timestamps and module names, and get_logger() convenience function for all modules to use.</action>
  <verify>Call setup_logging() with different levels, verify log files created with proper formatting, call get_logger(__name__) and use it for info/warning/error messages</verify>
  <done>src/core/logging.py exists with working setup and logger functions</done>
</task>

<task type="auto">
  <name>Task 3: Implement configuration management</name>
  <files>src/core/config.py</files>
  <action>Create src/core/config.py with load_config() function that uses python-decouple or os.environ to load environment variables, supports .env files, and returns Config dataclass with all application settings (database URL, log level, environment, etc.).</action>
  <verify>Create .env.test file with test values, call load_config(), verify Config object populated correctly with defaults and overrides</verify>
  <done>src/core/config.py exists with working config loading from environment</done>
</task>

</tasks>

<verification>
Verify all modules can use new exception and logging patterns by updating one existing module (ceiling_panel_calc.py) to use new imports and replace 3-4 print() statements with logger calls.
</verification>

<success_criteria>
Core modules exist and provide unified error handling, logging configuration, and configuration management. All original print statements replaced. No hardcoded exceptions remain.
</success_criteria>

<output>
After completion, create .planning/phases/02-architecture-3d/{phase}-{plan}-SUMMARY.md
</output></content>
<parameter name="filePath">/home/tomas/Ceiling Panel Spacer/.planning/phases/02-architecture-3d/02-01-PLAN.md